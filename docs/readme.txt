docs/mariadb/docker-entrypoint.sh
    copied from MariaDB /usr/local/bin

docs/mariadb/docker-inspect-mariadb.json
    docker inspect of the mariadb image
    shows what Cmd and Entrypoint are set to

docs/traceroute.txt
    output from running traceroute 8.8.8.8 on macOS to find other IP addresses to ping
